# Ketcher 汉化与深色模式适配研究报告

> 日期: 2026-05-12
> 仓库: QiXinia/ketcher_zh (fork 自 epam/ketcher)

---

## 一、项目概况

Ketcher 是一个基于 TypeScript + React 的开源化学结构编辑器，采用 monorepo 架构：

| 包名 | 职责 |
|---|---|
| `ketcher-core` | 核心渲染引擎（SVG）、化学数据模型 |
| `ketcher-react` | 微分子编辑器 UI（工具栏、对话框、右键菜单等） |
| `ketcher-macromolecules` | 大分子编辑器 UI（聚合物、肽链、RNA/DNA） |
| `ketcher-standalone` | 独立后端集成 |
| `demo` / `example` | 演示应用 |

---

## 二、汉化方案分析

### 2.1 现状：无国际化支持

Ketcher **没有任何 i18n 基础设施**：

- 未安装任何国际化库（无 `react-i18next`、`react-intl`、`i18next` 等）
- 无翻译文件（无 `locales/`、`i18n/` 目录）
- 无语言切换 UI
- **所有用户可见文本均为硬编码英文字符串**

### 2.2 硬编码字符串分布

英文字符串主要分布在以下位置：

| 位置 | 示例 | 数量估算 |
|---|---|---|
| `ketcher-react/src/script/ui/action/index.ts` | `title: 'Clear Canvas'`, `'Open...'`, `'Undo'` | ~40 条 |
| `ketcher-react/src/script/ui/action/server.ts` | `'Layout'`, `'Aromatize'`, `'Dearomatize'` | ~15 条 |
| `ketcher-react/src/script/ui/views/modal/components/` | 对话框标题、按钮、说明文字 | ~100+ 处 |
| `ketcher-react/src/script/ui/views/components/ContextMenu/` | `'Delete'`, `'Mark as a...'`, `'Copy'` | ~30 条 |
| `ketcher-react/src/script/ui/views/toolbars/` | 工具提示、标签 | ~50 条 |
| `ketcher-react/src/components/Dialog/Dialog.tsx` | `'OK'`, `'Cancel'` | ~10 条 |
| `ketcher-macromolecules/src/` | 大分子编辑器 UI 文本 | ~40 条 |

**总计约 300+ 处硬编码英文字符串需要提取和翻译。**

### 2.3 推荐汉化方案

#### 方案 A：引入 react-i18next（推荐）

**步骤：**

1. **安装依赖**
   ```bash
   pnpm add i18next react-i18next -F ketcher-react -F ketcher-macromolecules
   ```

2. **创建翻译文件结构**
   ```
   packages/ketcher-react/src/i18n/
     index.ts          # i18n 初始化配置
     locales/
       en.json         # 英文（从代码提取）
       zh.json         # 中文翻译
   ```

3. **提取字符串并替换**
   - 将 `title: 'Clear Canvas'` 改为 `title: t('action.clearCanvas')`
   - 将 JSX 中的 `'Delete'` 改为 `{t('contextMenu.delete')}`
   - 需要修改约 50+ 个源文件

4. **在入口组件中注入 Provider**
   ```tsx
   // packages/ketcher-react/src/Editor.tsx
   import { I18nextProvider } from 'react-i18next';
   import i18n from './i18n';
   // 包裹 <I18nextProvider i18n={i18n}>...</I18nextProvider>
   ```

5. **添加语言切换 UI**
   - 在工具栏或设置菜单中添加语言选择器

**优点：** 生态成熟、支持复数/插值/命名空间、社区方案多
**缺点：** 需改动大量源文件、与上游合并时冲突多

#### 方案 B：快速硬编码替换（不推荐，但最快）

直接在源文件中将英文字符串替换为中文，不引入 i18n 框架。

**优点：** 零依赖、改动直接
**缺点：** 失去英文版本、无法切换语言、与上游同步困难

#### 方案 C：构建时字符串替换

通过 Webpack/Vite 插件在构建时根据目标语言替换字符串，源码保持英文。

**优点：** 源码不变、可维护多语言
**缺点：** 需要额外的构建配置、调试困难

### 2.4 汉化工作量估算

| 任务 | 工作量 |
|---|---|
| 搭建 i18n 基础设施 | 1-2 天 |
| 提取并分类所有字符串 | 2-3 天 |
| 翻译为中文 | 1-2 天 |
| 替换源文件中的硬编码字符串 | 3-5 天 |
| 测试与调整 | 2-3 天 |
| **合计** | **约 10-15 个工作日** |

---

## 三、深色模式适配方案分析

### 3.1 现状：无深色模式支持

Ketcher **没有深色模式**，但部分架构已具备主题切换的潜力。

### 3.2 样式系统全景

Ketcher 存在 **三套并行的样式系统**：

| 系统 | 使用位置 | 技术 | 深色模式难度 |
|---|---|---|---|
| LESS 变量 | ketcher-react（微分子 UI） | LESS + CSS Modules | 中 |
| Emotion 主题 + CSS 变量 | ketcher-macromolecules（大分子 UI） | @emotion/styled + CSS Custom Properties | 低 |
| 硬编码常量 | ketcher-core（SVG 渲染引擎） | TypeScript 常量 | 高 |

### 3.3 各系统详细分析

#### A) ketcher-macromolecules —— 最佳基础

大分子编辑器已有完善的主题架构：

- **Emotion Theme 对象**：`packages/ketcher-macromolecules/src/theming/defaultTheme/defaultTheme.ts`，包含 60+ 颜色值
- **CSS 自定义属性**：`packages/ketcher-macromolecules/src/theme.less`，75+ CSS 变量
- **主题覆盖机制**：`Editor.tsx` 接受 `theme?: DeepPartial<EditorTheme>` prop，支持部分覆盖
- **MUI ThemeProvider**：已集成 Material UI 的主题系统

```tsx
// 已有的主题合并逻辑 (ketcher-macromolecules/src/Editor.tsx)
const editorTheme = theme ? merge(defaultTheme, theme) : defaultTheme;
const mergedTheme = merge(muiTheme, { ketcher: editorTheme });
```

**适配方案：** 创建 `darkTheme.ts`，覆盖默认主题的颜色值，通过已有的 `theme` prop 传入即可。

#### B) ketcher-react —— 需要改造

微分子编辑器使用 LESS 变量，无 CSS 自定义属性：

- **变量文件**：`packages/ketcher-react/src/style/variables.less`，60+ 变量
- **85+ 个 `.module.less` 文件**引用这些变量
- **MUI 已集成**：`App.tsx` 中使用 `createTheme()` + `ThemeProvider`

**适配方案：**
1. 将 LESS 变量迁移为 CSS 自定义属性（在 `:root` 和 `[data-theme="dark"]` 下定义不同值）
2. 或者通过 MUI 的 `createTheme({ palette: { mode: 'dark' } })` 配合 Emotion 覆盖

#### C) ketcher-core SVG 渲染 —— 最大挑战

SVG 渲染引擎中有大量硬编码颜色，无主题概念：

| 文件 | 硬编码内容 |
|---|---|
| `render/options.ts` | `stroke: '#000'`, hover `'#0097A8'`, 选中 `'#57FF8F'` |
| `renderers/BondRenderer.ts` | `'black'` 描边, `'#0097A8'` 悬停, `'#f5f5f5'` CIP 标签背景 |
| `renderers/BaseMonomerRenderer.ts` | `'white'`/`'black'`/`'#333333'` 文字颜色 |
| `domain/constants/elementColor.ts` | 111 个元素的固定颜色（`H: '#000000'`, `O: '#ff0d0d'`） |
| `render/renderers/constants.ts` | `SELECTION_COLOR = '#57FF8F'`, `UNRESOLVED_MONOMER_COLOR = '#585858'` |

**微分子模式（Raphael.js）：** 完全无主题参数，颜色通过 `.attr('fill', color)` 直接写入 SVG。
**大分子模式（D3.js）：** 部分渲染器接受 `theme` 参数（如 `PeptideRenderer.show(theme)`），但 `BondRenderer` 仍硬编码颜色。

**适配方案：**
1. 将 `RenderOptions` 扩展为主题感知（传入主题对象而非固定颜色）
2. 为 `ElementColor` 创建暗色版本（亮色背景上的元素色在暗色背景上可能不可读）
3. 修改 `BondRenderer`、`AtomRenderer` 等，从主题读取颜色

### 3.4 深色模式工作量估算

| 任务 | 工作量 |
|---|---|
| ketcher-macromolecules 暗色主题定义 | 2-3 天 |
| ketcher-react LESS 变量 → CSS 变量迁移 | 3-5 天 |
| ketcher-core SVG 渲染器主题化 | 5-8 天 |
| ElementColor 暗色适配 | 1-2 天 |
| MUI 暗色主题配置 | 1 天 |
| 主题切换 UI | 1 天 |
| 测试与视觉调整 | 3-5 天 |
| **合计** | **约 15-25 个工作日** |

---

## 四、推荐实施路径

### 阶段一：基础设施（第 1-2 周）

1. 引入 `react-i18next`，搭建 i18n 框架
2. 创建暗色主题定义文件（基于现有 `defaultTheme.ts`）
3. 将 ketcher-react 的 LESS 变量迁移为 CSS 自定义属性

### 阶段二：核心改造（第 3-4 周）

1. 提取所有硬编码字符串，创建 `zh.json` 翻译文件
2. 将 ketcher-core 的 SVG 渲染器改造为主题感知
3. 适配 ElementColor 暗色方案

### 阶段三：集成与测试（第 5 周）

1. 添加语言切换和主题切换 UI
2. 全面测试中文显示（文本溢出、字体渲染等）
3. 全面测试深色模式（对比度、可读性、化学结构清晰度）

### 与上游同步策略

- 汉化改动应集中在翻译文件中，源码改动最小化
- 深色模式改动应尽量通过主题对象和 CSS 变量实现，避免修改组件逻辑
- 建议定期从 upstream 合并，使用 `git merge upstream/master` 保持同步

---

## 五、技术参考

### 关键文件索引

| 用途 | 文件路径 |
|---|---|
| 动作标题（英文） | `packages/ketcher-react/src/script/ui/action/index.ts` |
| 服务端动作标题 | `packages/ketcher-react/src/script/ui/action/server.ts` |
| LESS 变量 | `packages/ketcher-react/src/style/variables.less` |
| 大分子主题定义 | `packages/ketcher-macromolecules/src/theming/defaultTheme/defaultTheme.ts` |
| 大分子 CSS 变量 | `packages/ketcher-macromolecules/src/theme.less` |
| 大分子主题类型 | `packages/ketcher-macromolecules/src/theming/defaultTheme/theme.types.ts` |
| SVG 渲染选项 | `packages/ketcher-core/src/application/render/options.ts` |
| 元素颜色表 | `packages/ketcher-core/src/domain/constants/elementColor.ts` |
| 键渲染器常量 | `packages/ketcher-core/src/application/render/renderers/constants.ts` |
| MUI 主题配置 | `packages/ketcher-react/src/script/ui/App/App.tsx` |
| 大分子编辑器入口 | `packages/ketcher-macromolecules/src/Editor.tsx` |

---

## 六、结论

| 维度 | 现状 | 难度 | 工作量 |
|---|---|---|---|
| 汉化 | 无 i18n，300+ 硬编码英文字符串 | 中 | ~10-15 人天 |
| 深色模式 | 无支持，三套并行样式系统 | 高 | ~15-25 人天 |

**建议优先做汉化**（投入产出比高），深色模式可分阶段推进，先适配大分子编辑器（已有主题基础），再改造微分子编辑器和 SVG 渲染引擎。
